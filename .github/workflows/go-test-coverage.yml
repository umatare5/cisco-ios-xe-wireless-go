name: Coverage
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  coverage:
    uses: umatare5/common/.github/workflows/go-test-coverage.yml@d2ecc6dad30688e2c0bd5676d741c359194e23a7 # v0.11.0
    with:
      go_version: "1.24.5"
      coverage_threshold: 95

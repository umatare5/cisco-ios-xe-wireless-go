package core

// StandardTimeout for normal operations (references the DefaultTimeout from client.go for compatibility)
const (
	// StandardTimeout for normal operations (same as DefaultTimeout for compatibility)
	StandardTimeout = DefaultTimeout
)
